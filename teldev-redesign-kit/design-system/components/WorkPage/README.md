**Sample composition** — the Work page: a `PageHero` with a project-handover `LayeredVisual`, two case study cards (each with a compact visual showing its headline result), two testimonials, the `CTABanner`, and the footer.

**Placeholders.** Both case studies, their metrics and the testimonials are illustrative. Publish only real projects, with the client's permission.
