Multi-line text entry — same visual language as `Input`, resizable vertically only.

**Anatomy/states/accessibility.** Identical contract to `Input` (see its guidelines); the only difference is `rows` (default 5) and `resize: vertical` so the control never grows wider than its container.
