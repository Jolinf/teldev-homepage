A row of partner and client wordmarks — real logos will replace these text placeholders.

**Anatomy.** Evenly spaced and `text-muted`-toned so no single logo outcompetes the page. Real logos are supplied as SVG assets and shown at a consistent optical size, never squeezed into a fixed box.

**Motion.** Logos fade in left to right, 60ms apart, once. No scrolling marquee.

**Accessibility.** Each logo image needs `alt` naming the organisation, and the strip is introduced by a heading ("Trusted by").
