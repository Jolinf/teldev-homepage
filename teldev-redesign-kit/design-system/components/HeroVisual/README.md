The homepage preset of `LayeredVisual`: a photograph with three status cards — a Microsoft 365 migration at 86%, a helpdesk ticket resolved in 42 minutes, and an automation run (invoice in → approved → sent). Click "Replay animation" in the preview. For every other page, use `LayeredVisual` directly with cards that fit that page.

**Props.** `photoSrc`, `photoAlt`, `decorative`.

**Photograph brief.** A TELDEV engineer working alongside a client in a real Lagos office, natural light, candid. The left third of the frame sits under the cards, so keep the subject centre-right.

**Card copy.** The figures are illustrative. Swap in real examples once TELDEV has them; never inflate them.

**Motion and layout.** As `LayeredVisual`.
