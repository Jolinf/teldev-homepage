**Sample composition** — the blog index: a `PageHero` whose visual is the featured article's image (16:9) with a featured-post card over it, then a grid of `BlogCard`s (4:3 images on the index) revealing in a stagger, and pagination.

**Placeholders.** Article titles and dates are examples.
