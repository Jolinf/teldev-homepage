A summarised case study card linking to the full write-up.

**Anatomy.** A compact `LayeredVisual` (4:3 photo with one `MetricCard` showing the project's headline result, bottom-right), a `success`-toned "Case study" badge, `h4` title, `small` summary, a `TextLink` through to the full story.

**Props.** `title`, `summary`, `note` (photo art direction), `metricIcon`, `metricLabel`, `metricValue`.

**Motion.** The card lifts on hover and its photo zooms slightly (3%); the metric card fades up when it scrolls into view. No idle drift on compact visuals.

**Layout.** Stacked on mobile, image and text side by side from `md` up.
