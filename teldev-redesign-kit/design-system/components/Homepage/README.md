**Sample composition** — the full homepage: header, the split hero with its animated visual, the four services (revealing on scroll), "How we work" with connector lines that draw in, partner logos and testimonials, the CTA banner with its floating card, and the footer. Use the header's theme toggle for dark mode.

**Composition notes.** The hero's two paths are equal in visual weight, so neither the Hire us nor the Partner with us audience reads as the primary one. Below the hero, each section uses one staggered `Reveal` group, never more.
